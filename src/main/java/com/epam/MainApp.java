/*
package com.epam;

import com.epam.context.AppDataContext$class;
import com.epam.context.AppDataContextImpl;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class MainApp {

    public static void main(String[] args) {
        var springContext = SpringApplication.run(MainApp.class, args);
        var repositoryContext  = springContext.getBean(AppDataContextImpl.class);
        System.out.println(repositoryContext.cachedUsers());
    }
}
*/